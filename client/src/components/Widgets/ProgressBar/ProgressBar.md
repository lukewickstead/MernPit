```js
// Only inclusion of the <ProgressBar/> is required
const MemoryRouter = require('react-router').MemoryRouter;
<MemoryRouter initialEntries={["/Email"]} initialIndex={0}>
    <ProgressBar/>
</MemoryRouter>
```